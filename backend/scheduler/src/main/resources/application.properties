spring.application.name=AlertWIP
# Dynamiczna pula
app.executor.core-size=5
app.executor.max-size=50
app.executor.queue-capacity=100
server.port=10000

# Sciezka do skryptów
app.scripts.path=scripts/

# Dane polaczenia
#spring.datasource.url=jdbc:mysql://localhost:3306/alertdb
#spring.datasource.username=root
#spring.datasource.password=

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Wylacza kompresje
server.compression.enabled=false
spring.mvc.async.request-timeout=-1

# Wygenerowany 256-bit klucz NIE MOZE TRAFIC NA PROD
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
# 24h w milisekundach (przeniesiona do SettingsSeeder)
#jwt.expiration=86400000
# 7dni w milisekundach (przeniesiona do SettingsSeeder)
#jwt.refresh-token.expiration=604800000

# Konfiguracja AD (przeniesiona do SettingsSeeder)
#ad.domain=example.com
#ad.url=ldap://ldap.forumsys.com:389
#ldap.baseDn=dc=example,dc=com
#ldap.userDnPattern=uid={0}

# Konfiguracja Redis
#spring.data.redis.host=localhost
#spring.data.redis.port=6379


# Konfiguracja MySQL Docker
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/alertdb
spring.datasource.username=root
spring.datasource.password=

# Konfiguracja Redis Docker
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=6379