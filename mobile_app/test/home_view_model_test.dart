//Prosty i mało praktyczny dla nas test wygenerowany przez automat, wrzucam jako placeholder pliku/folderu testowego i do testu flutter test
import 'package:flutter_test/flutter_test.dart';
//import 'package:alert_app/logic/home_view_model.dart';

void main() {
  test('latestAlerts should return max 3 items sorted by date', () {
    // Arrange
    //final viewModel = HomeViewModel();

    // Act
    //final result = viewModel.latestAlerts();

    // Assert
    //expect(result.length, lessThanOrEqualTo(3));
    //if (result.length >= 2) {
      // Sprawdzenie czy pierwszy jest nowszy od drugiego
      //expect(result[0].createdAt.isAfter(result[1].createdAt), true);
    //}
  });
}